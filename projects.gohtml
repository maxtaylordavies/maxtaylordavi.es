<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Projects</title>
    <link rel="stylesheet" href="/styles/projects.css">
    <link rel="stylesheet" href="/styles/tags.css">
    <style>
        html {
            background: {{ .Theme.Background }};
            color: {{ .Theme.Color }};
        }
        .title {
            border-bottom-color: {{ .Theme.Color }}
        }
        .nav-link {
            background: {{ .Theme.Color }}
        }
    </style>
</head>
<body>
<div class="container">
    <div class='nav-links'><a class='nav-link' href='/?theme={{ .Theme.Name }}'>home</a></div>
    <div class="body">
        <p class="title">All Projects</p>
        <ul class="list">
            {{range .Projects}}
                <div class="list-item">
                    <a class="project-title" href="/project?id={{.Id}}">{{.Title}}</a>
                    <ul class="tags">
                        {{range .Tags}}
                            <div class="tag {{.}}">{{.}}</div>
                        {{end}}
                    </ul>
                </div>
            {{end}}
        </ul>
    </div>
</div>
</body>
</html>